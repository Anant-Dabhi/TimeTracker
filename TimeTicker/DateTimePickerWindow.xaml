<Window x:Class="TimeTicker.DateTimePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TimeTicker"
        mc:Ignorable="d"
       Title="Select Date Time"
        Width="300"
        Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <StackPanel Margin="10">

        <!-- Date -->
        <!-- Time -->
        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
            <ComboBox x:Name="hourBox" Width="60"/>
            <TextBlock Text=":" VerticalAlignment="Center" Margin="5,0"/>
            <ComboBox x:Name="minuteBox" Width="60"/>
            <ComboBox x:Name="ampmBox" Width="60" Margin="5,0"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,15,0,0">
            <Button Content="OK" Width="70" Click="Ok_Click"/>
            <Button Content="Cancel" Width="70" Margin="5,0,0,0" Click="Cancel_Click"/>
        </StackPanel>

    </StackPanel>
</Window>
